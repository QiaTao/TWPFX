<Page
    x:Class="TWPFX_Gallery.Views.Pages.Design.TRotateAnimationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:codeblock="clr-namespace:TWPFX.Controls.TextBlock.CodeBlock;assembly=TWPFX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TWPFX_Gallery.Views.Pages.Design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:wpfui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    Title="TRotateAnimationPage"
    d:DataContext="{d:DesignInstance local:TRotateAnimationPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Text="旋转动画演示" />

            <TextBlock
                Margin="0,20,0,10"
                FontSize="16"
                FontWeight="SemiBold"
                Text="基本旋转动画" />

            <!--  基本旋转动画演示  -->
            <Border
                Margin="0,10"
                Padding="20"
                Background="{DynamicResource TAreaBackgroundDefault}"
                BorderBrush="{DynamicResource TAreaBorderDefault}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="14"
                        Text="点击按钮触发旋转动画" />

                    <StackPanel Margin="0,10" Orientation="Horizontal">
                        <Border
                            x:Name="rotateTarget1"
                            Width="80"
                            Height="80"
                            Background="LightBlue"
                            BorderBrush="Blue"
                            BorderThickness="2"
                            CornerRadius="8">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Text="旋转" />
                        </Border>

                        <StackPanel Margin="20,0,0,0">
                            <ui:Button
                                x:Name="rotateButton1"
                                Margin="0,5"
                                Click="RotateButton1_Click"
                                Content="动画测试" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>


        </StackPanel>
    </ScrollViewer>
</Page> 