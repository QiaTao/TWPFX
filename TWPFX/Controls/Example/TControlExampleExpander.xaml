<UserControl
    x:Class="TWPFX.Controls.Example.TControlExampleExpander"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:codeblock="clr-namespace:TWPFX.Controls.TextBlock.CodeBlock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Height="31"
            FontWeight="Bold"
            SnapsToDevicePixels="True"
            Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Header}"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="ClearType"
            UseLayoutRounding="True" />
        <!--  控件展示区域  -->
        <Border
            Grid.Row="1"
            Background="#fdfdfd"
            BorderBrush="{StaticResource TAreaBorderDefault}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentPresenter
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="16"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Content="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Content}" />
                <CheckBox
                    x:Name="DisableCheckBox"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="16"
                    Checked="DisableCheckBox_Checked"
                    Content="{DynamicResource DisableExample}"
                    Unchecked="DisableCheckBox_Unchecked" />
                <Border
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Background="#fefefe"
                    BorderBrush="{StaticResource TAreaBorderDefault}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8">
                    <Expander
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Header="{DynamicResource SourceCode}"
                        IsExpanded="False">
                        <codeblock:TCodeBlock
                            Grid.Row="1"
                            Padding="0"
                            Background="Transparent"
                            BorderThickness="0"
                            Code="{Binding RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=1}, Path=Code}"
                            Lang="{Binding RelativeSource={RelativeSource AncestorType=UserControl, AncestorLevel=1}, Path=Lang}" />
                    </Expander>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl> 